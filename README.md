This AI accepts an array of 256 uint8 values as input.
Edit the NewIntray definition to supply input in this form.

It outputs an array of 4 uint8 values to the outTrayExisting.
Edit the Score definition to set how outputs are scored.
A higher score should be given for a better output.

Increase the number of ticks to increase the allowed processing time per generation.
Left unedited, it will learn to estimate square roots.

Requires Matplotlib and Numpy.
